type: object
name: EventInfoSection
label: Event Info Section
groups:
  - sections
fields:
  - type: model
    name: logo
    label: 'Logo (overrides event config logo)'
    default: null
    models:
      - Image
  - type: string
    name: description
    label: 'Description (overrides event config description)'
    default: ''
  - type: date
    name: date
    label: 'Date (overrides event config date)'
    default: null
  - type: string
    name: venue
    label: 'Venue (overrides event config venue)'
    default: ''
  - type: model
    name: cta
    label: Call to action
    default: null
    models:
      - Link
  - type: model
    name: banner
    label: Banner image
    default: null
    models:
      - Image
